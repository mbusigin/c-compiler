/**
 * optimizer.c - Optimization passes (stub)
 */

#include "optimizer.h"
#include "../common/util.h"

IRModule *optimizer_optimize(IRModule *module, OptLevel level) {
    UNUSED(module);
    UNUSED(level);
    return module;
}
