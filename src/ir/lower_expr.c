/**
 * lower_expr.c - Expression lowering module
 *
 * This module will contain expression lowering functions migrated from lowerer.c.
 * Currently, expression lowering remains in lowerer.c as static functions.
 * Code will be migrated incrementally as part of ongoing refactoring.
 *
 * Planned functions to migrate:
 * - lower_expression()
 * - lower_binary_expr()
 * - lower_unary_expr()
 * - lower_call_expr()
 * - lower_identifier_expr()
 * - lower_literal_expr()
 * - lower_cast_expr()
 * - lower_member_access_expr()
 * - lower_pointer_member_access_expr()
 */

#include "../ir/ir.h"
#include "../parser/ast.h"
#include "../common/util.h"

// Expression lowering utilities specific to this module will be added here
// during incremental refactoring from lowerer.c

// This file is a placeholder for future expression lowering code migration
